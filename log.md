# vish execution log

Append-only operational log. Entries are added automatically by `.claude/hooks/append-log.sh` on every Bash/Write/Edit tool use (PostToolUse hook configured in `.claude/settings.json`). Manual session summaries are added as level-3 headings.

Format of auto-entries: `` - `UTC timestamp` **Tool** — description — `command-or-path` ``

---

### 2026-04-23 — Phase 0 kickoff

- Session started. CLAUDE.md + ROADMAP.md reviewed.
- Plan drafted and persisted to `docs/PHASE_1_PLAN.md`.
- `vish-engineer` agent created at `.claude/agents/vish-engineer.md` with §10 invariants + mandatory two-pass (correctness → performance) workflow.
- Logging hook installed at `.claude/settings.json` + `.claude/hooks/append-log.sh`. Hook script self-test passed. **Activates on next session start** — Claude Code reads settings.json at session init, not hot-reload.
- `git init` on branch `main`; initial commit `2731074` (10 files, 1001 insertions) with planning + infra baseline.
- 5 of 6 OSS references shallow-cloned into `third-party/reference/` (gitignored). SHAs recorded in `third-party/SOURCES.md`. Commit `1bc62b6`.
- sparkle-updater reference: upstream URL `ahkohd/tauri-plugin-sparkle-updater` returned 404. Deferred to pre-Week-4 — either locate a live fork or bridge `sparkle-project/Sparkle` directly via objc2. Noted in `SOURCES.md` followups.
- Phase 0 spike S3 (global-hotkey ⌥Space) launched in background via `general-purpose` subagent, instructed to load and obey `.claude/agents/vish-engineer.md` as its operating contract (actual `vish-engineer` subagent not yet runtime-registered — will be directly invokable next session).
- **Spike S3 result: PASSED (compile-time).** Agent delivered 35-line `src/main.rs` using `global-hotkey 0.7` + `objc2-app-kit 0.3` `NSApplicationActivationPolicy::Accessory`, registers ⌥Space, event thread logs `id` + `state` via structured `tracing`. Pass 2 removed two unnecessary `unsafe` blocks (objc2-app-kit 0.3 exposes `setActivationPolicy`/`run` as safe). Verified independently: `cargo check` + `cargo clippy --all-targets -- -D warnings` both clean. **Runtime validation left to human:** `cd third-party/spikes/s3-hotkey && cargo run`, press ⌥Space, expect `hotkey fired … state=Pressed/Released` log lines. AX prompt on launch = spike failed → trigger risk R2 (⌥⌘Space fallback).

### 2026-04-23 — Phase 0 continuation (same session, hook still dormant)

- Verified hook is still inactive this session (no auto-entries added between turns). Confirmed same-session state.
- **S2+S4 merged into one combined spike.** Rather than two sequential spikes, one crate at `third-party/spikes/s2-gpui-stack/` forces the full UI-side dependency graph `[gpui + gpui-component + objc2 + objc2-app-kit + dispatch2]` into a single compile unit. More informative failure mode than two separate checks. Delegated to vish-engineer-surrogate (general-purpose agent bound to the vish-engineer.md contract) running in background; agent instructed to WebFetch gpui-component's pinned `gpui` rev rather than guessing, with 3-retry cap and versions-that-worked recorded in a RESULTS.md for CLAUDE.md §2 pin update.
- **S1 script committed** at `third-party/spikes/s1-mlx/run.sh` + README. Uses `mlx-community/SmolLM2-135M-Instruct-4bit` (~80MB) since the spike is protocol-only — real Llama-3.1-8B-Q4 download deferred to Phase 3. Script running in background; first run includes one-time `uv run --with mlx-lm` install. Pass criterion: OpenAI SSE `data: {...}` chunks + `data: [DONE]` terminator.
- **S1 background run FAILED: `uv not on PATH`.** Exhaustive search (standard bin dirs, `~/.local/bin`, pyenv/conda/poetry, brew list) confirms `uv` is not installed on this machine despite the initial questionnaire answer. Not installing it automatically — user decides. Unblock: `curl -LsSf https://astral.sh/uv/install.sh | sh` (Astral's one-shot installer → `~/.local/bin/uv`). README updated with the pre-req note and failure-mode expectation.
- **S2+S4 result: PASSED (compile-time), first try.** Agent delivered 15-line compile probe at `third-party/spikes/s2-gpui-stack/` forcing [`gpui + gpui-component + objc2 + objc2-app-kit + objc2-foundation + dispatch2`] into one compile graph. `cargo check` + `cargo clippy --all-targets -- -D warnings` both clean (verified independently, 0.44s incremental). Pass 2: no changes (15-line probe, no hot paths). **Three findings, all in `RESULTS.md`:**
  1. `gpui-component` does not pin `gpui` → double-clone of Zed monorepo if we pin `gpui` separately. **Adopted Option A in CLAUDE.md §2:** declare `gpui = { git = "…zed" }` with NO `rev`, let Cargo.lock carry reproducibility. Single clone.
  2. `objc2-*` 0.3-line crates default to zero bindings exposed. Every type needs its feature + every inherited type's feature (e.g., `NSApplication` needs `["NSApplication","NSResponder"]`). **Encoded as a §2 rule** so Week 2+ work doesn't churn on feature-flag retries.
  3. `gpui` default features include `wayland`/`x11` on macOS — harmless, target-gated no-ops. No override needed.
- **CLAUDE.md §2 updated** (commit `1bfd0c1`) with the locked pins and the feature-flag rule.
- Commits this run: `c46e13e` (S1 script) → `1f0299a` (log) → `c367cf8` (S1 uv finding) → `1bfd0c1` (S2+S4 + §2 pins).
- Sparkle candidates identified: `hankbao/sparkle-updater` (native Rust, best fit), `crates.io/tauri-plugin-sparkle-updater v0.2.2` (Tauri-specific but uses exact objc2 pins), `sparkle-project/Sparkle` (ultimate fallback). Documented in `SOURCES.md`. Week 4 unblocked. Commit `699e11d`.
- **Phase 1 Week 1 COMPLETE.** Agent scaffolded Cargo workspace with 10 members (apps/vish, apps/vishd, 8 crates/vish-*) + rust-toolchain.toml + scripts/{dev,reset-tcc}.sh. [workspace.dependencies] uses the minimal Week-1 set plus Phase 0 UI stack pins. All four exit gates green: cargo build/test/clippy clean, `./scripts/dev.sh` emits `INFO vish: vish starting` and exits 0. Pass 2: no changes (stubs are trivially minimal). Commit `c902dcc`. Agent flagged a minor brittleness: Week-1-style assertion via `head -5` can fail on cold cache; use `grep -q "vish starting"` in future gate scripts.
- **Agent evolution:** added "No speculative APIs" rule to `.claude/agents/vish-engineer.md` based on Phase 0 S2+S4 learning (agent guessed `App::new` + `Queue::main`, both wrong). Commit `5ce753f`. Will apply durably from next invocation (and every future session).
- **Phase 1 Week 2 Task A (sketch) COMPLETE.** Agent delivered NOTES.md + `todo!()` stubs in `crates/vish-macos/`. **Load-bearing finding:** GPUI already creates an `NSPanel` subclass for `WindowKind::PopUp` (confirmed: `zed-reference/crates/gpui_macos/src/window.rs:75, 80, 129, 672-674`). Reduces Week 2 from "build NSPanel subclass" to ~150 lines of augmentation: hotkey bridge + `MoveToActiveSpace` OR-in + vibrancy wrapper. Public API: `Hotkey::{register, on_fire}`, `panel::configure(&W, Options)`, `vibrancy::apply(&W)` — all on `impl HasWindowHandle` via raw-window-handle 0.6. workspace.deps added: `global-hotkey 0.7`, `window-vibrancy 0.6`, `raw-window-handle 0.6 [std]`. objc2-app-kit features: `NSApplication, NSPanel, NSResponder, NSView, NSWindow`. 7 open questions in NOTES.md — advisor validated the sketch ("only Q1 is a real decision"); all 7 greenlit with agent's proposed answers. Commit `0fa2bfc`.
- **Phase 1 Week 2 Task B launched.** Agent in background filling in the three module bodies + wiring into `apps/vish/src/main.rs` (NSApp Accessory policy, `gpui::Application::new().run`, `WindowKind::PopUp` window, hotkey bridge via `std::thread` + `tokio::mpsc::unbounded_channel` + `cx.spawn` toggle loop). Advisor-required addition: NSWindow→NSPanel downcast guard in `panel::configure` (use `downcast_ref::<NSPanel>()`, new `Error::NotPanel` variant). Runtime validation (press ⌥Space, observe no-AX-prompt + focus-preservation) is human-gated and will be the natural session pause.

---
- `2026-04-24T04:46:47Z` **Bash** — Hook self-test — `echo test `
